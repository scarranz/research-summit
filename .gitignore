env.js
.env
.env.*
node_modules/
.DS_Store
_preview-*.html

# Confidential fund data — NEVER commit to the public repo
data/
data.zip
# Spec contains real fund returns / AUM — keep local until repo is private
docs/fund-returns-spec.md
# Local dev helper
dev-server.ps1
# Massive API key for any local dashboard proxy — never commit (repo is public)
**/.massive-key
# Local-only covered-calls dashboard scratch dirs (gitignored keys live here)
playground/

# Reference screenshots (Chip Stock Investor) — local only, do not commit
nvidia-map-reference/

# Payment-networks industry dashboard + extracted data — local only, do not commit
payments-industry-reference/

# Robotics working docs + Bloomberg SPLC data — local only (the portal-facing
# dashboard is robotics-industry.html + img/robotics/, which ARE committed)
robotics-research/
