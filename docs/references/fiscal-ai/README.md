# Fiscal.ai UI references

Drop screenshots of the Fiscal.ai screens you like here (PNG/JPG) — table layouts,
chart styles, color usage, density toggles, anything worth replicating in the
portal's Results section (Deep Dive ▸ Evolution ▸ Results).

Claude reads the images in this folder when iterating on the Results UI: after
uploading, just say "revisa las referencias de Fiscal" and mention what to focus
on (e.g. "la tabla", "los headers", "el hover").

Suggested naming: `table-quarterly.png`, `chart-segments.png`, `hover-crosshair.png` …
